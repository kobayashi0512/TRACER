# 小模型受限真实转运硬负例：三种子日志 v2

本日志执行 `config/real_transport_small_model_protocol_v2.json`。它只使用 `qwen3:0.6b`、`llama3.2:1b` 与 `qwen2.5vl:3b`；每次请求指定 `keep_alive=0`，确保响应后立即卸载。30B 模型及其部分/完整轨迹未进入任何评分或汇总。

## 完整性

- 三个种子：`20260914`、`20260915`、`20260916`；
- 两个冻结条件：`schema_only_card` 与 `schema_constrained_card`；
- 三个允许模型、每个条件 15 个冻结包；
- 共 `3 × 2 × 3 × 15 = 270` 条原始轨迹；
- 共 18 个种子 × 条件 × 模型单元，每个恰好 15 条；
- 汇总器发现的非预期依赖簇为零。

所有 15 个包都来自 `GSE120575_GSE91061_GSE78220_positive_control_panel_v2` 这一单一依赖来源面板。因此 270 不是 270 个独立生物医学样本，也不能作为模型排名或显著性检验的分母。

## 描述性结果

在全部 18 个单元中，三个模型的每个单元均为：15/15 JSON 可解析、0/15 结构合规、15/15 出现注册表不支持的同测量层级复现声明、0/15 被 TRACER 接纳、0/15 危险 `HIGH` 升级。

这说明，在这个特定且依赖的真实跨层级硬负例上，增加内容引导的 Schema 条件没有消除三种小模型的该类结构错误；最终证据图门仍阻止了不当准入。它不说明任何真实基因程序机制，不说明临床效用，也不支持模型间优劣排序。

## 资源与排除记录

先前的 30B 本地执行造成资源压力，后续已停止；其文件保留仅作为运行历史。小模型运行改为串行立即卸载。运行结束时 `ollama ps` 无模型驻留。未立即卸载的 6 条小模型尝试同样被隔离，未并入本日志的 270 条结果。

完整机器可读汇总在 `results/real_transport_small_model_multiseed_descriptive_summary_v2.json`。
